package com.prabin.swarmedge.benchmark;

/**
 * Scenario dispatcher. {@link BenchmarkHarness} applies impairment, runs one scenario,
 * removes the impairment even after a failure, and writes {@code research/raw/<runId>/}.
 */
public final class BenchmarkRunner {

    private BenchmarkRunner() {
    }
}
